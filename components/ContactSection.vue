<template>
  <section id="contact" class="bg-navy-900 py-24 px-10 lg:px-16">
    <div class="max-w-6xl mx-auto">
      <p class="section-tag mb-4">
        <span class="text-accent-blue">/</span> Contact
      </p>
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-16 items-end">

        <!-- Left: Info -->
        <div>
          <h2 class="text-3xl lg:text-4xl font-bold leading-tight mb-6 text-gray-900 dark:text-white">
            Let's Work Together
          </h2>
          <p class="text-gray-600 dark:text-gray-400 text-sm leading-relaxed mb-10">
            Have a project in mind or just want to say hello? I'm always open to discussing new opportunities, freelance work, or collaborations.
          </p>

          <div class="space-y-5 mb-10">
            <div class="flex items-center gap-4">
              <div class="w-10 h-10 rounded-lg bg-gray-100 dark:bg-white/5 flex items-center justify-center shrink-0">
                <svg class="w-4 h-4 text-accent-blue" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
                </svg>
              </div>
              <div>
                <p class="text-xs text-gray-500 mb-0.5">Email</p>
                <a href="mailto:prolajumokeoni@gmail.com" class="text-sm text-gray-900 dark:text-white hover:text-accent-blue transition-colors">prolajumokeoni@gmail.com</a>
              </div>
            </div>

            <div class="flex items-center gap-4">
              <div class="w-10 h-10 rounded-lg bg-gray-100 dark:bg-white/5 flex items-center justify-center shrink-0">
                <svg class="w-4 h-4 text-accent-blue" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433a2.062 2.062 0 01-2.063-2.065 2.064 2.064 0 112.063 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/>
                </svg>
              </div>
              <div>
                <p class="text-xs text-gray-500 mb-0.5">LinkedIn</p>
                <a href="https://www.linkedin.com/in/prolajumokeoni/" target="_blank" rel="noopener noreferrer" class="text-sm text-gray-900 dark:text-white hover:text-accent-blue transition-colors">linkedin.com/in/prolajumokeoni</a>
              </div>
            </div>

            <div class="flex items-center gap-4">
              <div class="w-10 h-10 rounded-lg bg-gray-100 dark:bg-white/5 flex items-center justify-center shrink-0">
                <svg class="w-4 h-4 text-accent-blue" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M12 .297c-6.63 0-12 5.373-12 12 0 5.303 3.438 9.8 8.205 11.385.6.113.82-.258.82-.577 0-.285-.01-1.04-.015-2.04-3.338.724-4.042-1.61-4.042-1.61C4.422 18.07 3.633 17.7 3.633 17.7c-1.087-.744.084-.729.084-.729 1.205.084 1.838 1.236 1.838 1.236 1.07 1.835 2.809 1.305 3.495.998.108-.776.417-1.305.76-1.605-2.665-.3-5.466-1.332-5.466-5.93 0-1.31.465-2.38 1.235-3.22-.135-.303-.54-1.523.105-3.176 0 0 1.005-.322 3.3 1.23.96-.267 1.98-.399 3-.405 1.02.006 2.04.138 3 .405 2.28-1.552 3.285-1.23 3.285-1.23.645 1.653.24 2.873.12 3.176.765.84 1.23 1.91 1.23 3.22 0 4.61-2.805 5.625-5.475 5.92.42.36.81 1.096.81 2.22 0 1.606-.015 2.896-.015 3.286 0 .315.21.69.825.57C20.565 22.092 24 17.592 24 12.297c0-6.627-5.373-12-12-12"/>
                </svg>
              </div>
              <div>
                <p class="text-xs text-gray-500 mb-0.5">GitHub</p>
                <a href="https://github.com/prolajumokeoni" target="_blank" rel="noopener noreferrer" class="text-sm text-gray-900 dark:text-white hover:text-accent-blue transition-colors">github.com/prolajumokeoni</a>
              </div>
            </div>
          </div>

          <!-- Book a Consultation -->
          <div class="border border-accent-blue/20 rounded-xl p-6 bg-accent-blue/5">
            <p class="text-xs text-accent-blue font-semibold uppercase tracking-widest mb-2">Free Consultation</p>
            <h3 class="text-gray-900 dark:text-white font-bold text-lg mb-2">Book a 30-min call</h3>
            <p class="text-gray-600 dark:text-gray-400 text-sm mb-5">Let's talk about your project, goals, or how I can help. Pick a time that works for you.</p>
            <button
              data-cal-link="priscilla-olajumoke-oni-myhwqz/secret"
              data-cal-namespace="secret"
              data-cal-config='{"layout":"month_view","useSlotsViewOnSmallScreen":"true"}'
              class="w-full bg-accent-blue hover:bg-accent-blue-hover text-white text-sm font-semibold py-3 rounded-lg transition-colors duration-200 flex items-center justify-center gap-2"
            >
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
              </svg>
              Book a Free Consultation
            </button>
          </div>
        </div>

        <!-- Right: Form -->
        <form class="space-y-5" @submit.prevent="handleSubmit">
          <div class="grid grid-cols-1 sm:grid-cols-2 gap-5">
            <div>
              <label class="block text-xs text-gray-600 dark:text-gray-400 mb-2">Name</label>
              <input
                v-model="form.name"
                type="text"
                placeholder="Your name"
                required
                class="w-full bg-gray-50 dark:bg-white/5 border border-gray-200 dark:border-white/10 rounded-lg px-4 py-3 text-sm text-gray-900 dark:text-white placeholder-gray-400 dark:placeholder-gray-600 focus:outline-none focus:border-accent-blue transition-colors"
              />
            </div>
            <div>
              <label class="block text-xs text-gray-600 dark:text-gray-400 mb-2">Email</label>
              <input
                v-model="form.email"
                type="email"
                placeholder="your@email.com"
                required
                class="w-full bg-gray-50 dark:bg-white/5 border border-gray-200 dark:border-white/10 rounded-lg px-4 py-3 text-sm text-gray-900 dark:text-white placeholder-gray-400 dark:placeholder-gray-600 focus:outline-none focus:border-accent-blue transition-colors"
              />
            </div>
          </div>
          <div>
            <label class="block text-xs text-gray-600 dark:text-gray-400 mb-2">Subject</label>
            <input
              v-model="form.subject"
              type="text"
              placeholder="What's this about?"
              required
              class="w-full bg-gray-50 dark:bg-white/5 border border-gray-200 dark:border-white/10 rounded-lg px-4 py-3 text-sm text-gray-900 dark:text-white placeholder-gray-400 dark:placeholder-gray-600 focus:outline-none focus:border-accent-blue transition-colors"
            />
          </div>
          <div>
            <label class="block text-xs text-gray-600 dark:text-gray-400 mb-2">Message</label>
            <textarea
              v-model="form.message"
              rows="5"
              placeholder="Tell me about your project..."
              required
              class="w-full bg-gray-50 dark:bg-white/5 border border-gray-200 dark:border-white/10 rounded-lg px-4 py-3 text-sm text-gray-900 dark:text-white placeholder-gray-400 dark:placeholder-gray-600 focus:outline-none focus:border-accent-blue transition-colors resize-none"
            />
          </div>
          <button
            type="submit"
            :disabled="sending"
            class="w-full bg-accent-blue hover:bg-accent-blue-hover disabled:opacity-60 disabled:cursor-not-allowed text-white text-sm font-semibold py-3 rounded-lg transition-colors duration-200 flex items-center justify-center gap-2"
          >
            <span>{{ sending ? 'Sending...' : 'Send Message' }}</span>
            <svg v-if="!sending" class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
            </svg>
          </button>
          <p v-if="sent" class="text-xs text-green-400 text-center">Message sent! I'll get back to you soon.</p>
          <p v-if="error" class="text-xs text-red-400 text-center">Something went wrong. Please try again or email me directly.</p>
        </form>

      </div>
    </div>
  </section>
</template>

<script setup lang="ts">
import emailjs from '@emailjs/browser'

const form = reactive({ name: '', email: '', subject: '', message: '' })
const sending = ref(false)
const sent = ref(false)
const error = ref(false)

async function handleSubmit() {
  sending.value = true
  error.value = false
  try {
    await emailjs.send(
      'service_ajsoxqa',
      'template_cl86k97',
      {
        from_name: form.name,
        from_email: form.email,
        subject: form.subject,
        message: form.message,
      },
      'LdDYRtCeES2Th7O_A'
    )
    sent.value = true
    Object.assign(form, { name: '', email: '', subject: '', message: '' })
    setTimeout(() => { sent.value = false }, 4000)
  } catch {
    error.value = true
  } finally {
    sending.value = false
  }
}
</script>
